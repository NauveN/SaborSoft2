@page
@model SaborSoft2.Pages.Reservas.CreateModel

@{
    ViewData["Title"] = "Registrar reserva";
}

<h1 class="mb-3">Registrar reserva</h1>

<div class="row">
    <div class="col-lg-8">
        <div class="card shadow-sm">
            <div class="card-body">
                <form method="post">
                    <div asp-validation-summary="All" class="text-danger mb-3"></div>

                    <h5 class="mb-3">Datos del cliente</h5>
                    <div class="row g-3 mb-4">
                        <div class="col-md-4">
                            <label asp-for="Reserva.Cedula" class="form-label">Cédula</label>
                            <input asp-for="Reserva.Cedula" class="form-control" />
                            <span asp-validation-for="Reserva.Cedula" class="text-danger"></span>
                        </div>
                        <div class="col-md-4">
                            <label asp-for="NombreCliente" class="form-label">Nombre</label>
                            <input asp-for="NombreCliente" class="form-control" />
                            <span asp-validation-for="NombreCliente" class="text-danger"></span>
                        </div>
                        <div class="col-md-4">
                            <label asp-for="CorreoCliente" class="form-label">Correo electrónico</label>
                            <input asp-for="CorreoCliente" class="form-control" type="email" />
                            <span asp-validation-for="CorreoCliente" class="text-danger"></span>
                        </div>
                    </div>

                    <h5 class="mb-3">Datos de la reserva</h5>
                    <div class="row g-3">
                        <div class="col-md-3">
                            <label asp-for="Reserva.Fecha" class="form-label">Fecha</label>
                            <input asp-for="Reserva.Fecha" class="form-control" type="date"
                                   min="@DateTime.Today.ToString("yyyy-MM-dd")" />
                            <span asp-validation-for="Reserva.Fecha" class="text-danger"></span>
                        </div>

                        <div class="col-md-3">
                            <label asp-for="Reserva.Cantidad" class="form-label">Personas</label>
                            <input asp-for="Reserva.Cantidad" class="form-control" min="1" />
                            <span asp-validation-for="Reserva.Cantidad" class="text-danger"></span>
                        </div>

                        <div class="col-md-6">
                            <label asp-for="Reserva.TipoReservaId" class="form-label">Tipo de reserva</label>
                            <select asp-for="Reserva.TipoReservaId" class="form-select"
                                    asp-items="Model.TiposReservaSelectList">
                                <option value="">-- Seleccione tipo de reserva --</option>
                            </select>
                            <span asp-validation-for="Reserva.TipoReservaId" class="text-danger"></span>
                        </div>

                        <div class="col-md-3">
                            <label asp-for="MesaCodigo" class="form-label">Mesa (solo se muestran las disponibles)</label>
                            <select asp-for="MesaCodigo" class="form-select"
                                    asp-items="Model.MesasSelectList">
                                <option value="">-- Seleccione mesa --</option>
                            </select>
                            <span asp-validation-for="MesaCodigo" class="text-danger"></span>
                        </div>

                        <div class="col-md-3">
                            <label asp-for="DisponibilidadId" class="form-label">Horario</label>
                            <select asp-for="DisponibilidadId" class="form-select"
                                    asp-items="Model.DisponibilidadesSelectList">
                                <option value="">-- Seleccione horario --</option>
                            </select>
                            <span asp-validation-for="DisponibilidadId" class="text-danger"></span>
                        </div>
                    </div>

                    <div class="mt-4 d-flex justify-content-between">
                        <a asp-page="Index" class="btn btn-outline-secondary">Volver al listado</a>
                        <button type="submit" class="btn btn-primary">Confirmar reserva</button>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <div class="col-lg-4 mt-4 mt-lg-0">
        <div class="card border-info">
            <div class="card-header bg-info text-white">Información</div>
            <div class="card-body small">
                <ul class="mb-0">
                    <li>La fecha debe ser hoy o posterior.</li>
                    <li>Cada cédula solo puede tener <strong>una reserva por día</strong>.</li>
                    <li>Solo se muestran las <strong>mesas disponibles</strong> para la fecha y horario seleccionados.</li>
                    <li>Al guardar se genera un <strong>código alfanumérico</strong> de 6 caracteres para el cliente.</li>
                </ul>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
